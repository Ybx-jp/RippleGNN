# Claims inventory (draft) — raw list, no schema

**Date:** 2026-08-27 · **Component:** research method · **Status:** open.

Schema-agnostic inventory of every claim and prediction in the project that will need a
ledger entry. The taxonomy is being settled elsewhere; this is the raw list only.

Sources read in full: exchanges modelling consultation, round 1 (dl R1), measurement consultation, round 1
(eval-methodology R1), modelling consultation, round 2 (dl R2), measurement consultation, round 2 (eval-methodology
R2); `lab/001`–`lab/005`; `MANIFEST.md`; `experiments/README.md`; and the two durable
docs those exchanges' operator constraints came from (`CLAUDE.md`, public `README.md`).

Nothing here is edited into an existing note. Two categories, kept separate.

---

## A. Predictions carrying an explicit number

These are what make the consultation channel scorable. Completeness matters more here
than anywhere else in this document.

### A.1 Scored — an explicit confidence or probability was stated

| # | Statement, as worded | Number | Expert | Ticket | What has since borne on it |
|---|---|---|---|---|---|
| A1 | "my current expectation is that L3 kills exact k-hop local refresh at k≥2 on any heavy-tailed graph, and that the project's real result is the sub-k fidelity curve plus the cost-blowup characterization, not a refresh engine" | **65%** | dl | modelling consultation, round 1 §8 | **Superseded by the expert itself.** In R2 §5 dl split this into two claims (A2, A3) on the grounds that it was always two claims and should have been stated as such. The 65% figure is the parent of both and should be scored as retired-by-split, not as resolved. Note it was put on the record explicitly so it "cannot be retrofitted" — the split is a partial defeat of that intent and is worth ledgering as a channel-behaviour observation in its own right. |
| A2 | "Exact k-hop local refresh degenerates to effectively-global recomputation at k ≥ 2 on any heavy-tailed graph." | **78%** (raised from 65) | dl | modelling consultation, round 2 §5(a) | **Threatened by lab/005, not refuted.** The uplift 65→78 was justified by JKNet's expander result (Xu et al., JKNet) supplying "a mechanism I did not have in round 1" — but the *degree-tail* half of the argument is what lab/005 undercuts: under mean aggregation error is exactly degree-invariant at fixed stale fraction, so hubs are the most robust node per stale neighbour, not the most fragile. lab/005 states the residual question correctly: whether heavy tails hurt now reduces to whether hub neighbourhoods acquire a higher stale *fraction*, which is an empirical question about mutation locality rather than a consequence of the degree distribution. The expander half of the mechanism is untouched by lab/005 and still stands. **Never put back to `dl`** (lab/005 "Open"). |
| A3 | "The project's real result is the sub-k fidelity curve plus the cost-blowup characterisation, rather than a refresh engine." | **70%** (held, with amendment) | dl | modelling consultation, round 2 §5(b) | **Amended by lab/001 at the time of statement, sharpened again by lab/004.** dl's own amendment: "'cost blowup' is memory residency and feature I/O, not wall-clock FLOPs. I had the trap right and the mechanism wrong." lab/004 sharpens further — the governing quantity is `edges × hidden_dim`, not nodes. dl also noted a partially offsetting downward pressure it chose not to act on: GAS's Lemma 1 makes the fidelity curve *predicted* rather than discovered, lowering the value of the first half. |
| A4 | "on ogbn-products and Reddit, realised error at refresh radius r will be more than an order of magnitude below the Lemma-1 bound at every r > 0, and the gap will widen with endpoint degree — because the bound's `\|𝒩(v)\|` factor is worst-case over a neighbourhood whose realised contributions largely cancel." | **60%** | dl | modelling consultation, round 2 §5 (new, on the record before R1 ran) | **Two independent things bear on it, both post-dating it.** (i) lab/005: the `\|𝒩(v)\|` factor is a *sum*-aggregator artifact; GraphSAGE/PyG default to mean, and the degree term drops out of Theorem 2 entirely. The stated *mechanism* ("worst-case over a neighbourhood whose contributions cancel") is therefore the wrong mechanism for the model in use, and the "gap widens with endpoint degree" clause loses its basis — under mean aggregation there is no degree factor left to be loose about. (ii) lab/004: ogbn-products and Reddit-at-128 both OOM, so the two named substrates are partly unavailable; Reddit-at-64 is the surviving one. **This prediction is scoreable only after deciding whether it is read against sum or mean aggregation** — that decision is itself unmade and belongs in the ledger. |

### A.2 Unscored — a ranking or a preference stated without a number

Recorded separately because they are not scoreable as stated, but they are commitments
the channel made and can be graded qualitatively.

| # | Statement | Expert | Ticket | Standing |
|---|---|---|---|---|
| A5 | dl's five ordered experiments: R1 invalidation-set/influence combinatorics > R2 sub-k sweep vs GAS bound > R3 memory-residency cliff > R4 scheduling under budget > R5 mutation-class asymmetry. Ranked on "expected information per CPU-second". | dl | modelling consultation, round 2 §3 | Unrun. lab/005 argues R1 measures the wrong quantity for a mean aggregator (set *size* vs set *fraction*), which if right demotes R1 as specified. |
| A6 | eval-methodology's five ordered experiments: R1 metric qualification grid > R2 non-inferiority study > R3 geometric-drift-predicts-decision-change > R4 dataset-moderator replication > R5 cost-axis measurement. Ranked on "expected validity per CPU-second". | eval-meth | measurement consultation, round 2 §1 | Unrun. Its R1 and dl's R1 are different experiments with the same rank; the collision is unadjudicated. |
| A7 | Dataset tier ranking: ogbn-products and Reddit tier A; ogbn-arxiv, Yelp, Flickr, Cluster tier B; Cora/CiteSeer/PubMed tier C ("scheduling noise wearing a result's clothing"). | dl | modelling consultation, round 2 §4 | **Primary refuted by lab/004** — both tier-A entries OOM at 128-dim. Tier C confirmed by lab/004's sub-millisecond measurements. |
| A8 | Dataset ranking: tgbl-wiki primary, tgbl-review as R4 contrast, OGB link-property as fallback; hard rejection of Cora/CiteSeer/PubMed and of PPI/Reddit/Amazon2M (saturation). | eval-meth | measurement consultation, round 2 §2 | **Primary refuted by lab/004** — tgbl-wiki full recompute is 1.729 ms. The saturation objection to Reddit is *not* refuted and lab/004 explicitly leaves it standing. |
| A9 | Domain ranking: product co-purchase > social/forum > citation (control) > financial fraud (cite as motivation only) > road/infrastructure ("the trivial-success trap"). | dl | modelling consultation, round 2 §4 | Open; downstream of the unresolved dataset decision. |
| A10 | "Do not adopt a CTDG benchmark. Construct your mutation stream over a large static graph." | dl | modelling consultation, round 2 §4 | **Direct conflict with A8**, which ranks tgbl-wiki primary precisely because its split and negative-sampling protocol are prescribed rather than self-designed. Unadjudicated. |
| A11 | Ruling: conduct primary fidelity measurement under full-graph inference; report sampled fidelity as a mandatory second panel; report the difference as a third estimand. | eval-meth | measurement consultation, round 2 §3 | Accepted into `experiments/README.md` only in its first clause. The three-panel decomposition is not in the contract. |
| A12 | "the sharpest threat to your novelty" is GNNAutoScale/VR-GCN historical embeddings — later ruled: "Partial withdrawal. The novelty claim on 'stale neighbour with an error bound' is dead." | dl | R1 §5 G2, ruled in R2 §1 | The withdrawal is itself a scoreable channel event: predicted in R1, confirmed against the fed papers in R2. |
| A13 | R1 withdrawal: "On insert/delete asymmetry I withdraw the round-1 prediction that it is a theorem I can hand you." | dl | modelling consultation, round 2 §3/R5 | Retracted before it could be scored. Cause: the IVM classics never ingested (host allowlist), so the prediction had no source. |
| A14 | R1 withdrawal: "I withdraw the RBO recommendation from round 1 §5 G9 and will not cite it." | eval-meth | measurement consultation, round 2 §0 | Retracted; cause was procurement failure, not evidence. |
| A15 | Self-refutation: "my 'smaller dataset, more replays' advice is partly refuted… the correct form is many episodes on one adequately-sized graph, not many tiny graphs." | eval-meth | measurement consultation, round 2 §0 Correction 2 | **The pre-correction form is still what the public contract says.** See P1. |
| A16 | Self-refutation: "CKA is the wrong primary geometric measure, and this is measured, not argued… Use Orthogonal Procrustes distance instead." | eval-meth | measurement consultation, round 2 §0 Correction 1 | Corroborated by held literature. **Not wired into any repo document.** See B29. |
| A17 | "Your K=20 is defensible but unmotivated; k=10 has measured warrant and is cheaper." | eval-meth | measurement consultation, round 2 §0 | lab/002 used K=20. Unchanged. |
| A18 | "your measured 0.6208 floor is likely an optimistic bound" (because L1 used uniform degree, so hubness cannot arise). | eval-meth | measurement consultation, round 2 §0 Correction 3 | Untested. Directionally agrees with lab/002's own stated threat. |
| A19 | "in deployment, the sampler may contribute as much embedding instability as the entire refresh decision. If that reproduces on graphs, it is the most interesting result this project can produce." | eval-meth | measurement consultation, round 2 §3 | Untested; transferred from a word-embedding result, labelled by the expert as a transfer. |
| A20 | "the modal outcome you should expect": a simple heuristic (recency-ordering, EdgeBank) matches the clever policy, and that counts as the finding. | both | R1 §3c / R2 §2, and dl R4 | Untested. |

---

## B. Load-bearing claims

Columns: how established (**measured here** / **argued** / **cited** / **asserted**),
what depends on it, and current standing. Claims nothing depends on are kept and marked,
per the brief.

### B.1 Hardware and cost

| # | Claim (verbatim where possible) | Where | How established | What depends on it | Standing |
|---|---|---|---|---|---|
| B1 | "4 cores, not the GPU, is the binding constraint, because neighborhood sampling and mutation replay are CPU-bound." | Former `CLAUDE.md` / public README; **passed to both experts as a settled operator constraint** inside modelling consultation, round 1 and measurement consultation, round 1 | **Asserted, no basis.** lab/001: "it was written before it was measured." | Propagated into *both* round-1 answers. dl built probe L2 to "prove it with a profile before designing around it". eval-meth built its entire §4e cost-axis ruling on it (B35). | **Refuted** by lab/001. Corrected in both documents. The downstream contamination of two consultations is not itself ledgered anywhere. |
| B2 | "VRAM caps full-graph refresh at roughly 1-2M nodes" | lab/001 line 62; **still live in public `README.md` lines 66-67** | Measured here — but at mean degree 10 only | Dataset tier rulings in both R2 tickets; the "full recompute becomes impossible" framing | **Superseded by lab/004** (governing quantity is `edges × hidden_dim`; at mean degree 99 the ceiling arrives at 233k nodes). lab/001 carries the supersession banner. **The public README does not** — see P4. |
| B3 | "the ceiling is an edge count… ~0.51 GiB per million edges at 128-dim; between 15M and 20M edges" | lab/004:41 | Measured here | The admissible band (5M–15M edges at 128-dim); the choice of Reddit-at-64; the refutation of both experts' primaries | Standing. Caveat B41. |
| B4 | "an unsynchronized timing of a kNN kernel understated it by 173x, because it timed queue submission rather than compute" | `CLAUDE.md`:78, `experiments/README.md` | Measured here | The always-synchronize rule in both durable docs; every timing in lab/001 and lab/004 | Standing. |
| B5 | Full-recompute timings for named benchmarks: Cora 0.504 ms, CiteSeer 0.519 ms, tgbl-wiki 1.729 ms, Reddit-JODIE 2.099 ms, PubMed 2.047 ms; Reddit and ogbn-products OOM | lab/004:22-28 | Measured here, **on synthetic random graphs of the right size with published approximate degrees, not the real datasets loaded** | The refutation of eval-methodology's primary (A8) and the confirmation of dl's tier C | Standing with the stated caveat. |
| B6 | "at every scale that fits on this box, exact nearest-neighbor search is affordable, so no ANN index is needed… Exact search removes a confound rather than merely saving a dependency." | lab/001:51-55 | Measured here — 100k×128 in 1.10 s, 250k in **7.0 s** | Every rank-survival metric in the project; the removal of ANN recall error as a confound; dl's ANN-as-second-cache framing | **Under-examined.** Measured only to 250k. Reddit-at-64 has 232,965 nodes, so the chosen operating point sits at the measured edge, and the primary metric needs a kNN pass *per episode per arm*. See P8. |
| B7 | "Full recomputation never becomes slow on this hardware. It becomes *impossible*." | lab/001:39, `CLAUDE.md`, public README | Measured here | The reframing of the cost axis from latency to residency; dl's R3; eval-meth's R5 | Standing, and independently predicted by dl in R1 §2 from NBFNet's published wall-clock (lab/003 calls this the convergence-from-two-directions result). |
| B8 | "the manifest's premise… that recomputing every embedding after every change is expensive enough to be worth avoiding" | `MANIFEST.md` mission; named as the premise under test in lab/001:12-14 | **Asserted** as the project's founding premise | The existence of the project | **Substantially narrowed.** It survives only as memory-impossibility (B7) and only inside a band of roughly 5M–15M edges at 128-dim (B3). This is the single most load-bearing claim in the repo and its support is one afternoon of synthetic probes. |

### B.2 Fidelity measurement

| # | Claim | Where | How established | What depends on it | Standing |
|---|---|---|---|---|---|
| B9 | "Full-graph inference is bitwise reproducible: the floor is exactly zero." | lab/002:25 | Measured here — one graph, one model, untrained weights, one driver state | eval-meth's A11 ruling; `experiments/README.md`'s "Full-graph inference has a floor of zero and is preferred wherever it fits"; the argument that κ = p in the primary regime | **Generalized past its evidence** in the public contract. eval-meth R1 §7 Gap 1 explicitly asked for a verified bitwise-determinism check naming cuDNN autotuning and atomic scatter-adds; no note records that audit. See P2. |
| B10 | "on an unchanged graph it loses 37.9% of each node's top-20 neighborhood on average, with a worst case of 19 of 20 neighbors changed" while cosine reads 0.991 | lab/002:19-27 | Measured here | The rank-primary rule in `experiments/README.md`; dl's instruction to carry it forward as a standalone methodological finding; eval-meth's rejection of a cosine-drift sweep | Standing, and **corroborated from literature** by eval-meth R2 Correction 3 (sampling-order seed instability comparable to a real data change, on *trained* models) — which upgrades it past the "qualitative only" caveat lab/002 itself imposed. |
| B11 | "The floor is a property of the inference path, not of the model or the graph." | lab/002:42 | **Argued** from a two-cell measurement | The design rule to prefer full-graph inference; dl's "the fix is not to measure around that floor, it is to remove it" | Untested against a trained model or a heavy-tailed graph — lab/002 lists both as threats. |
| B12 | "Rank-based measures are primary. A geometric figure may accompany one but never substitutes for it." | `experiments/README.md` | Measured here (B10) + cited | Every fidelity number the project will report | Well supported. |
| B13 | "Stability metrics are reported chance-corrected… κ = (p − p_null)/(1 − p_null)", null from k≥5 unchanged-graph reruns, "also the control for the rotation and alignment problem" | `experiments/README.md`; from eval-meth R1 §2 | **Argued** (transferred from a first-party permuted-null precedent in another scope) | Every stability reading | **Partly mooted by B9**: in the primary full-graph regime p_null = 0, so κ = p and the k≥5 rerun null costs five runs to produce a constant. The contract requires it unconditionally. |
| B14 | "No-refresh… scores perfectly on any pure stability metric — it is maximally self-consistent because it does nothing. Any metric on which no-refresh wins is degenerate and must be discarded." | `experiments/README.md`; eval-meth R1 §3a | **Argued, deductive.** The expert flagged it: "this one is deductive and I am confident in it, but no source here measured it." | The falsifier-arm requirement for every probe | Deductively sound *for a pure self-consistency metric under deterministic inference*. Under sampled inference the reference itself resamples, so no-refresh does **not** score perfectly — the claim is stated in the contract without that scope restriction. |
| B15 | "'Local refresh approximates full recomputation' is confounded between locality genuinely holding and the mutations being too small to move anything." | `experiments/README.md`; eval-meth R1 §3b | **Argued** by structural analogy to a defense-off control | The destructive-positive-control requirement | Standing; the analogy is explicit and the expert labelled it as such. |
| B16 | "an untrained GNN was already showing performance that is competitive with DeepWalk" → every headline run carries an untrained-weights control | `experiments/README.md`; dl R1 T1 | **Cited** | The untrained-weights control arm | Standing. Note the irony ledgered nowhere: lab/002 and lab/005 both *ran on* untrained weights, i.e. the control condition was used as the measurement condition. |
| B17 | "CKA requires deleting 97% of principal components before registering a detectable dissimilarity… Use Orthogonal Procrustes distance instead." | eval-meth R2 §0 Correction 1 | **Cited** (measured third-party) | Metric selection for the geometric half of B12 | **Wired to nothing.** No repo document names Procrustes, CKA, or the no-whitening rule. See P9. |
| B18 | "any similarity index that is invariant to orthogonal transformation can be made invariant to invertible linear transformation by orthogonalizing the columns" → "a QR/whitening step before comparison silently destroys your measurement" | eval-meth R2 §0 | **Cited** (theorem) | The pre-registration prohibition on whitening / fitted general-linear alignment | Wired to nothing in the repo. |
| B19 | "Ten nearest neighbors performs approximately as well as a higher number" / k=5 top-performing as predictor of downstream disagreement | eval-meth R2 §0 | **Cited** | The choice of K in every rank metric | lab/002 used K=20 and nothing has changed it. |

### B.3 Locality, the error model, and the reframe

| # | Claim | Where | How established | What depends on it | Standing |
|---|---|---|---|---|---|
| B20 | "For a k-layer full-neighborhood message-passing model, this never happens, and the answer is not empirical — it is definitional. Exact k-hop refresh does not *approximate* global recomputation; it *equals* it, bit for bit." | dl R1 §1 | **Argued** from GraphSAGE Algorithm 2 (Hamilton et al., GraphSAGE, Algorithm 2); dl labels it "follows from the source's argument, never measured by it" | **The entire accepted reframe.** MANIFEST's locality question declared to have no empirical content; the r-sweep replaces it; lab/003 records the acceptance as one of round one's two headline corrections | Accepted without independent verification on this box. It is scoped to *full-neighborhood deterministic* aggregation and is **false under sampling** — dl says so in the same section ("with sampling turned on, 'exact' stops existing"), but the reframe is carried forward without the scope restriction attached. See P10. |
| B21 | "The interesting object is… the size distribution of the k-hop reverse-reachable set… **This is a property of the degree distribution, not of the model.**" | dl R1 §1(a) | **Argued** | dl's R1 experiment; prediction A2; the mixing-time independent variable | **Directly contradicted in its stated form by lab/005**: for a mean aggregator the governing quantity is the invalidation set as a *fraction* of the neighbourhood, which makes it a property of the model's aggregator after all. |
| B22 | Theorem 2, hand-fed at 100% anchoring: `‖h̃_v^(L) − h_v^(L)‖ ≤ Σ ε^(ℓ)·k₁^(L−ℓ)·k₂^(L−ℓ)·\|N(v)\|^(L−ℓ)` | lab/005:10 | **Cited**, verbatim, from the hand-fed span | dl's R2 headline (realised/bound ratio); prediction A4 | Standing as a citation. |
| B23 | "the \|N(v)\| factor comes from **sum** aggregation… mean or max admits a much tighter bound" → measured: "Under mean aggregation the error is **exactly degree-invariant at fixed stale fraction**, to six significant figures across two orders of magnitude of degree." | lab/005:12, 46 | **Cited** (the remark) + **measured here** (the behaviour) | Threatens A2 and A4; reframes dl's R1; makes the target claim degree-independent if it survives | Standing, with lab/005's four stated threats (one layer, one perturbation magnitude, star graph, untrained weights). The mechanism is called "exact and structural"; the magnitudes are explicitly not load-bearing. |
| B24 | "SAGEConv's default in PyG is mean" | lab/005:18-21 | **Verified in code** | Everything in B23 applies to the project as configured | Standing. |
| B25 | "the influence distribution I_x for any node x is equivalent, in expectation, to the k-step random walk distribution" (JKNet Thm 1) | dl R2 §2 | **Cited** | dl's R1 design (influence mass computable with sparse mat-vecs, no model); "predicts the fidelity curve before you run a single GNN forward pass" | Standing. |
| B26 | "Random walks starting inside an expander converge rapidly in O(log\|V\|) steps to an almost-uniform distribution" → on expander-like graphs influence does not decay with r | dl R2 §2 | **Cited** | The 65→78 uplift on A2; Reddit framed as "the negative-result arm"; mixing time / expansion named as the real independent variable rather than domain | Standing, and untouched by lab/005 — this is the half of A2's mechanism that survives. |
| B27 | "negatively-curved edges are exactly the edges whose mutation has the *smallest* reverse-reachable set, and positively-curved ones the largest… a testable, curvature-based prioritisation heuristic that nobody has proposed for refresh" | dl R2 §2 | **dl's own inference, explicitly labelled**, plus an absence-of-prior-work claim from scan | A proposed prioritisation arm in R1/R4 | Untested. The absence half should be phrased "not found in the current scan" per `CLAUDE.md`. |
| B28 | "the minimal hidden dimension to fit radius r grows exponentially with r; even d = 512 can empirically fit r = 7 at most" → "Real models are shallow because deep ones do not work", so k ∈ {1,2,3} is the regime the field operates in, not a budget convenience | dl R2 §2 | **Cited** | The defensibility of restricting k; pre-empts "why not k=8" in the write-up | Standing. Load-bearing on the write-up rather than on any measurement. |
| B29 | "Your r-sweep is a one-parameter family of GAS batches. Lemma 1 already covers it." | dl R2 §1 | **dl's inference from the source's argument**, explicitly labelled ("they never state it as a refresh radius") | The novelty withdrawal; the "replication-plus-transfer" positioning; the decision not to claim novelty | Standing as an inference. Nothing independent has tested it. |
| B30 | "The bound is worst-case and nobody measured its tightness… Ratio-of-realised-to-bound as a function of r and degree is an unoccupied, cheap, defensible measurement." | dl R2 §1 | **Absence claim from scan** | dl's R2 — the project's proposed headline | An absence claim is the weakest kind and this one carries the headline. Should be ledgered as "not found in the current scan". Further weakened by B23: for a mean aggregator there may be no degree-dependence to characterise. |
| B31 | "Their ε is not your ε… Yours is stale with respect to *graph mutation*. Weight drift is global and smooth; mutation drift is sparse and heavy-tailed." | dl R2 §1 | **dl's inference**, labelled | The claim that anything remains after the novelty withdrawal | Untested and it is the load-bearing survivor of the withdrawal. |
| B32 | MANIFEST line 103: mutation types "should not be assumed equivalent" — insert/delete/feature asymmetry | `MANIFEST.md`:103 | **Asserted.** dl R1 predicted it from IVM as a theorem, then withdrew: "Treat insert/delete asymmetry as an open empirical question, not a borrowed prediction." | dl's R5; eval-meth's adequacy grid (mutation type × probe) | Open. The only source that landed (DBToaster) says "deletions are duals of insertions and are omitted entirely" — no asymmetry claim. |

### B.4 Experiment contract and statistics

| # | Claim | Where | How established | What depends on it | Standing |
|---|---|---|---|---|---|
| B33 | "A non-inferiority claim without a pre-specified margin is unfalsifiable" — with the measured figure that 158/273 (57.9%) of published margins do not report their derivation | `experiments/README.md`; eval-meth R1 §1c | **Cited** (measured third-party) + argued transfer | The margin-with-derivation requirement in every pre-registration | Standing. |
| B34 | "The margin is expressed as a fraction of the full-recompute-to-no-refresh gap on the same probe, less the measured seed-variance floor for that configuration." | `experiments/README.md` | **Argued** (synthesis vs point-estimate method) | Every pre-registration | **Presupposes that gap is non-zero and measurable on the chosen dataset, which has never been measured on any candidate.** Combined with the unresolved saturation objection (B39) this may be near-zero. See P5. |
| B35 | "on 4 shared cores, wall-clock is a measurement of your box's scheduler, not of the algorithm. Report FLOPs, nodes-revisited, and messages-passed as the primary cost axes" | eval-meth R1 §4e, restated as R5 in R2 | **Argued — and its stated premise is B1, which is refuted** | Nothing in the repo. The conclusion is separately supported by lab/001 (memory binds), but the argument as given rests on the four-cores claim | **Accepted nowhere.** `experiments/README.md` requires no cost instrumentation at all. The cost half of a cost/fidelity trade-off currently has no contract. See P9. |
| B36 | "More independent mutation episodes and more seeds buy more validity than more nodes per snapshot. **A smaller graph replayed over many episodes dominates the largest graph that fits in memory.**" | `experiments/README.md` | **Argued** from ICC/design-effect literature (measured third-party: ρ=0.017 → deff 1.527, n 128→84, power 80%→61%) | The unit-of-analysis rule; the dataset-size reasoning | **The bolded sentence was refuted by its own author** in eval-meth R2 Correction 2 ("the correct form is many episodes on one adequately-sized graph, not many tiny graphs"). The contract still carries the pre-correction form. See P1. |
| B37 | "per-node ρ across a shared checkpoint will be far above 0.017" | eval-meth R1 §4a | **The expert's guess**, explicitly not measured; the source says ρ "usually requires a pilot study" | Every power and episode-count calculation | The pilot (L4) has never been run. Any n in a future pre-registration is currently unbacked. |
| B38 | "Git history is not a timestamp on a repo you control" → pre-registration is timestamped by its **push** | `experiments/README.md`, `CLAUDE.md`, commit `3839e88` | **Argued**, with a first-party precedent (a concurrent session that rewrote history and dropped the remote) | The pre-registration protocol | Standing and already acted on. |
| B39 | "PPI, Reddit and Amazon2M are saturated — 83%/65%/90% of nodes used for training, an artificially small distribution shift; there is no headroom for a refresh effect to live in." | eval-meth R2 §2 | **Cited** (measured third-party) | The rejection of Reddit | **Live unresolved conflict.** lab/004 chose Reddit-at-64 on cost grounds and states explicitly: "That objection stands and is unaddressed." lab/004's own resolution is conditional — "either a task on Reddit that is not saturated, or accepting that the fidelity probes carry the result and the downstream task is decoration." |
| B40 | "inductive node ratio is your primary selection criterion and the surprise index is your moderator" | eval-meth R2 §2 | Statistics **cited**; the pairing **labelled by the expert as inferred, not measured** | The tgbl-wiki ranking (A8) | Its conclusion is refuted on cost grounds by lab/004, but the *criterion* is untouched and unapplied to any surviving candidate. |
| B41 | "Degrees here are published approximate values applied to synthetic random graphs of the right size, not the real datasets loaded. **The memory arithmetic is structural and will hold**; the timings will move somewhat." | lab/004:90-93 | **Asserted** (the bolded prediction), on top of a measurement | The entire admissibility band (B3) and both dataset refutations | Untested. The whole dataset ruling rests on synthetic uniform graphs standing in for heavy-tailed real ones. See P7. |
| B42 | "run.py… **is itself the analysis path**. An analysis re-derived beside the production code path is a second implementation that can be wrong on its own, and has been." | `experiments/README.md` | **Argued** from a first-party failure in another scope (a false 0-of-3049 finding corrected to 739-of-741) | The run.py contract | Standing. |
| B43 | "Adequacy is claimable over the declared grid, never over the domain of mutations." — with k-MR coverage instantiated as "each mutation type must associate with at least k qualified probes drawn from different estimand families", k=3 | eval-meth R2 §4 | **Cited** (criterion, validated across 7 programs / 68,000 MRs) + **argued** transfer, with the scale gap flagged by the expert | The adequacy argument for the whole project | **Wired to nothing.** No repo document names the grid, the coverage criterion, or the requirement that the mutation taxonomy be declared closed before the first run. |
| B44 | Faithfulness is four estimands (self-consistency, rank survival, downstream behavioural consistency, downstream task quality) that must never be collapsed | `experiments/README.md`; eval-meth R1 §1a | **Argued** from a first-party construct-validity failure | Every metric in the project | Standing — but eval-meth R2 §4 adds a caveat the contract does not carry: "Your grid measures whether probes discriminate. It does not measure whether the four estimands carve faithfulness correctly. That question stays open." |
| B45 | "15% of predictions on a sentiment analysis task can disagree due to training the embeddings on an accumulated dataset with just 1% more data" → full-recompute-vs-full-recompute churn is the denominator for every churn number | eval-meth R2 §1/R3 | **Cited** (measured third-party) | The interpretability of any downstream-churn result | Wired to nothing in the repo. |
| B46 | MANIFEST line 109: "At least one comprehensible applied domain should be used to validate that the mechanism provides practical value, not merely attractive embedding metrics." | `MANIFEST.md`:109 | **Asserted** (manifest premise) | **Nothing currently.** dl notes "none of the top three experiments requires a link-prediction metric at all — which is itself a reason to prefer them"; eval-meth's R3 is the only one that needs a downstream head, at rank 3 | Marked not-currently-load-bearing. At risk of silent abandonment, which the manifest would forbid if it were noticed. See P11. |
| B47 | MANIFEST line 71: "GraphSAGE is a strong initial candidate… but the project is not defined by GraphSAGE. Model choice is a research decision." | `MANIFEST.md`:71 | **Asserted**, and framed as a *soft* preference | Was believed to depend on nothing | **This turns out to be load-bearing and nobody noticed.** lab/005 shows the aggregator choice (mean vs sum) determines whether the error model has a degree term at all — i.e. whether A2, A4, B21 and dl's entire R1 are asking the right question. A "research decision" deferred as low-stakes is in fact the decision the error model rests on. See P6. |
| B48 | "Poursafaei et al. did not land… That paper is a direct threat to your evaluation design and you should read it before you write a single eval script." | dl R1 §5 G3, restated R2 §6 | Procurement failure recorded as a gap | Any link-prediction eval | It *did* land in the `eval-methodology` scope (`ripple-measurement` feed) but not in `dl`'s. dl states it as absent; the operator holds it in the other scope. **A cross-scope inconsistency that neither expert can see.** |
| B49 | lab/003's cost accounting: 34,144 / 44,419 chars, 34 / 63 validated citations, 31 documents, ~$23, ~95 minutes; "whether that trade is good is not yet decided by this note" | lab/003 | **Measured here** | The judgement on whether the consultation channel is worth running again | Explicitly held open by its own note. |
| B50 | "Nothing may cite a live dataset directory"; `datasets.jsonl` pins the registry, "the bytes are not" committed | `CLAUDE.md`, `experiments/README.md` | **Asserted** as protocol | Reproducibility of every experiment | Standing; no experiment exists to test it. |

---

## C. Postulates — flat declarative, never measured

The brief asks for the ones nobody has noticed. P1, P2, P5–P11 are, as far as this scan
can tell, not recorded anywhere else in the repo. P3 and P4 are the two named in the
brief, kept here for completeness.

| # | Postulate | Why it is one |
|---|---|---|
| **P1** | `experiments/README.md`: "A smaller graph replayed over many episodes dominates the largest graph that fits in memory." | **The expert who supplied it refuted it in the next round** (A15/B36) and the contract was never updated. It is a public, pushed document stating advice its own author has withdrawn, and it points the dataset decision at exactly the tiny benchmarks lab/004 then measured as unusable. |
| **P2** | `experiments/README.md`: "Full-graph inference has a floor of zero and is preferred wherever it fits." | Generalized from one lab/002 cell — one graph, one model, untrained weights, one driver state. eval-meth R1 §7 Gap 1 asked specifically for a *verified* bitwise-determinism check naming cuDNN autotuning and atomic scatter-adds ("run the same seed twice and diff, once, and print the result"). That audit was never run for any real configuration, and the contract now depends on the zero floor for its primary regime, its κ arithmetic, and its margin derivation. |
| **P3** | `CLAUDE.md` / README (former): "4 CPU cores, not VRAM, are the binding constraint." | Named in the brief. Asserted, refuted by lab/001 — and it was fed to both experts as settled fact, so its blast radius includes B35 and both round-1 cost rulings. |
| **P4** | Public `README.md` lines 66-67: "VRAM caps full-graph refresh at roughly 1-2M nodes, and past that, neighbor sampling on 4 cores is the constraint." | Named in the brief as superseded by lab/004 — **and it is still live in the public README**, which `CLAUDE.md`'s documentation rule ("delete ghosts, do not annotate them") forbids. lab/001 carries the supersession banner; the public document does not. |
| **P5** | The margin construction presupposes a measurable full-recompute-to-no-refresh gap. | `experiments/README.md` makes the gap the denominator of every margin. It has never been measured on any candidate dataset, and B39's unresolved saturation objection says it may be near-zero on the leading candidate. If it is, no pre-registration in the project can state a margin. |
| **P6** | "Model choice is a research decision" — treated as deferrable. | lab/005 makes the aggregator the governing term of the error model. The decision was filed as low-stakes and turns out to determine whether prediction A2, prediction A4, and dl's rank-1 experiment are well-posed. Nothing in the repo records that the model choice is now blocking. |
| **P7** | lab/004: "The memory arithmetic is structural and will hold." | Asserted, on synthetic uniform-degree graphs standing in for heavy-tailed real ones. The admissible band, both expert refutations, and the Reddit-at-64 choice all rest on it. A heavy tail changes the gather pattern — lab/004 says so and then predicts it will not matter. |
| **P8** | "Exact kNN is affordable at every scale that fits on this box." | Measured to 250k nodes at 7.0 s. The chosen operating point (Reddit, 232,965 nodes) is at that edge, and the primary rank metric needs a kNN pass per episode per arm. eval-meth's design calls for many episodes. The cost of the project's primary metric is effectively unbudgeted, and the one number available says it is ~7 s a shot. |
| **P9** | The cost axis has no instrumentation requirement. | Both experts ruled that wall-clock is the wrong unit and named the replacements (FLOPs, nodes revisited, messages passed, peak resident memory, fraction of graph touched). `experiments/README.md` requires none of them. `CLAUDE.md`'s built-but-never-used rule has an inverse here: a cost claim not recorded at the time of the run is unfalsifiable afterwards, and there is currently no contract that it be recorded. |
| **P10** | "Exact k-hop refresh equals global recomputation, bit for bit." | The reframe that reorganized the whole research surface, accepted from an argument, never verified on this box, and true only for deterministic full-neighbourhood aggregation. dl states the sampling exception in the same section; the reframe travels without it. Under the sampled second panel that eval-meth's A11 ruling requires, the premise of the r-sweep does not hold. |
| **P11** | MANIFEST line 109's applied-domain validation. | Nothing depends on it. Both experts' top-ranked experiments avoid downstream tasks; dl treats that as a *point in their favour*. A manifest requirement is being routed around by the experiment ranking without any document recording the tension. |

---

## Counts

- Predictions with an explicit number: **4** (A1–A4), of which 1 was retired by the
  expert's own split and 2 have post-hoc evidence bearing on them.
- Unscored rankings, preferences, withdrawals and self-refutations: **16** (A5–A20).
- Load-bearing claims: **50** (B1–B50). Of these, 3 are marked not-currently-load-bearing
  and kept per the brief (B46, and the not-yet-wired B17/B18/B43/B45 cluster).
- Postulates in flat declarative voice never measured: **11** (P1–P11), of which **9**
  are not recorded anywhere else in the repo (P1, P2, P5–P11).
