{
  "class": "declared verbatim_sha does not match the content",
  "known_good": false,
  "expect": [
    {
      "checker": "validate",
      "outcome": "fail",
      "where": "A0001 frontmatter verbatim_sha",
      "why": "validate.py recomputes the fingerprint from the canonicalized Scope and quotes and the declared value differs"
    }
  ]
}
